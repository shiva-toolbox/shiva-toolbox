# Optional. Defaults to "shiva" (the password of the existing local volume).
# Keep it alphanumeric: the value is interpolated into a connection URL.
#
# In production, set a real password here. Changing it later does not update an
# existing volume — run:
#   ALTER USER shiva WITH PASSWORD '...';
POSTGRES_PASSWORD=shiva
